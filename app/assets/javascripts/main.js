;(function () {
  "use strict"

  $(document).ready(function () {
    // We'll boot up our app here so the rest of our code has a chance to load first.

    new Vue({

      el: "body",

      data: {}

    })
  })
})();
